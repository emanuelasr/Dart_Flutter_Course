// ternary operator exists to shorten a simple if/else block.
// it takes three operands rather than the typical one or two the most operators use.
// for example:
void main() {
  int age = 36;
  String type = age > 16 ? 'adult' : 'child';
  print(type);
}
// LAST UPDATE IN THE DARTBASICS SECTION
