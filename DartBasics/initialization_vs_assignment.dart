void main() {
  // Initialization of the Variable is when you declare and put the information inside;
  //example:
  var name = 'emanuel';
  print(name); //Output: emanuel

  //Assingment is when you change the value inside the variable or you the data
  // are inserted manually by the user.
  //example:
  name = 'antonio'; // value changed inside the variable 'name'
  print(name); // Output > antonio
}
