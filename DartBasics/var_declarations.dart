void main() {
  /* 
  to declare a variable in dart, you have to use the 'var' keyword, and then
  choose a variable and infer a data inside. for example.
  */
  var age = 10;

  // or

  var name = 'Emanuel';

  /* you don't have to explicitly declare the type of the variable, the Dart itself
    are smart enough to know the type of the variable that you infer inside the variable.
     if it's a text (String type), a age (integer type), heigh (double type).
  */
}
